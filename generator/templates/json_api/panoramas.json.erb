<%=
  result = {}
  Panorama.all.keep_if{ |p| p.published && !p.exclusive }.each do |p|
    result[p.id] = {
      name: p.name,
      city: {
        id: p.city.id,
        name: p.city.name
      }
    }
  end
  result.to_json
%>