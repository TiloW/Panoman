<% # coding: utf-8 
%>

<h1>Bearbeiten: <%= @external_link.name %></h1>

<div id="sections">
  <ul>
    <li><a href="#tab-fields">Datensatz</a></li>
  </ul>

  <div id="tab-fields" class="section">
    <%= form_for(@external_link) do |f| %>
      <div class="field">
        <%= f.label :name, "Name" %>
        <%= f.text_field :name %>
      </div>
      <div class="field">
        <%= f.label :en_name, "Englischer Name" %>
        <%= f.text_field :en_name %>
      </div>
      <div class="field">
        <%= f.label :path, "Hotspot-Bereich" %>
        <%= f.text_field :path %>
      </div>
      <div class="field">
        <%= f.label :url, "URL" %>
        <%= f.url_field :url, :size => 85 %>
      </div>
      <div class="field">
        <%= f.label :en_url, "Englische URL" %>
        <%= f.url_field :en_url, :size => 85 %>
      </div>
      <div class="field">
        <%= f.hidden_field :panorama_id %>
      </div>
      <div class="actions">
        <%= f.submit "Speichern" %>
      </div>
    <% end %>
  </div>
  
  <div class="section">
    <% @panorama = @external_link.panorama %>
    <%= render 'layouts/panorama' %>
  </div>

</div>
